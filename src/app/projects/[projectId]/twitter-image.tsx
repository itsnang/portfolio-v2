// X/Twitter cards reuse the generated wireframe card, otherwise the project
// would inherit the site-wide twitter:image (/cover.jpg) from root metadata.
export { default, alt, size, contentType } from "./opengraph-image";
